// atoms.js
import { atom } from 'jotai';

export const dialogsAtom = atom([]);
export const currentDialogIndexAtom = atom(-1);
export const titoAnimatingAtom = atom(false);
export const itzelAnimatingAtom = atom(false);
